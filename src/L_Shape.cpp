#include "L_Shape.h"

L_Shape::L_Shape(Board* board)
{
	/*
	board->addChild(this);

	blocks.push_back(new Block(this, new Point(0, 0)));
	blocks.push_back(new Block(this, new Point(0, 1)));
	blocks.push_back(new Block(this, new Point(0, 2)));
	blocks.push_back(new Block(this, new Point(1, 2)));

	setSize(blockSize * 3);*/

	//_board->activeShape = this;
}